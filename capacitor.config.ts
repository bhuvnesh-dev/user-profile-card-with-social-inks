import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'user-profile-card-with-social-links',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
